import { PageTitle } from "../molecules/PageTitle";
import styled from "styled-components";
import { ColorTheme } from "../../style/ColorTheme";

export const FilesLayout = () => {
  return <PageTitle>ファイル</PageTitle>;
};

const { palette } = ColorTheme;
