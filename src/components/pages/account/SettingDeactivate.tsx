import type { FC } from "react";
import { AccountDeleteLayout } from "../../templates/AccountDeleteLayout";

export const SettingDeactivate: FC = () => {
  return <AccountDeleteLayout title={"アカウント削除"} />;
};
