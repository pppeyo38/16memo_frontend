import styled from "styled-components"

export const Input = () => {
	return (
		<StyledInput></StyledInput>
	)
}

const StyledInput = styled.input`
	color: #cccccc;
	font-family: futura;
  padding: 9px 13px;
`;